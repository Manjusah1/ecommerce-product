export interface Product {
    id: number;
    title: string;
    description: string;
    price: number;
    category: string;
    rating: number; // Simplified rating based on instruction: "rating"
    image: string;
}
